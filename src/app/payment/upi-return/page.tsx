import HomePage from "../../page";

// A redirect is only a navigation signal. The storefront fetches order status
// from the API using the saved ownership token before displaying confirmation.
export default HomePage;
